name: Publish Docker Image

on:
  push:
    branches:
      - main

jobs:
  push_to_registry:
    name: Push Docker Image to Docker Hub
    runs-on: ubuntu-latest


    steps:
      - name: check out the repo
        uses: actions/checkout@v3

      - name: log in docker
        uses: docker/login-action@v1
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Build and push Docker image
        uses: docker/build-push-action@v2
        with:
          context: ./client
          push: true
          tags: zubairsource/client

      - name: log in docker
        uses: docker/login-action@v1
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Build and push Docker image
        uses: docker/build-push-action@v2
        with:
          context: ./server
          push: true
          tags: zubairsource/server

      - name: log in docker
        uses: docker/login-action@v1
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Build and push Docker image
        uses: docker/build-push-action@v2
        with:
          context: ./nginx
          push: true
          tags: zubairsource/nginx
# name: build mern image and push to docker hub

# on:
#   push:
#     branches: [ main ]

# jobs:
#   build-and-deploy-back:
#     name: Server
#     runs-on: ubuntu-latest
#     steps:
#     - name: checkout code
#       uses: actions/checkout@v2
#     - name: build and push docker image
#       uses: docker/build-push-action@v1
#       with:
#         username: ${{secrets.DOCKER_USERNAME}}
#         password: ${{secrets.DOCKER_PASSWORD}}
#         repository: zubairsource/mern_server
#         dockerfile: server/Dockerfile
#         tags: latest
#   build-and-deploy-front:
#     name: Client
#     runs-on: ubuntu-latest
#     needs: build-and-deploy-back
#     steps:
#     - name: checkout code
#       uses: actions/checkout@v2
#     - name: build and push docker image
#       uses: docker/build-push-action@v1
#       with:
#         username: ${{secrets.DOCKER_USERNAME}}
#         password: ${{secrets.DOCKER_PASSWORD}}
#         repository: zubairsource/mern_client
#         dockerfile: client/Dockerfile
#         tags: latest
#   build-and-deploy-nginx:
#     name: Nginx
#     runs-on: ubuntu-latest
#     needs: build-and-deploy-front
#     steps:
#     - name: checkout code
#       uses: actions/checkout@v2
#     - name: Build and push Docker image
#       uses: docker/build-push-action@v2
#       with:
#         username: ${{secrets.DOCKER_USERNAME}}
#         password: ${{secrets.DOCKER_PASSWORD}}
#         context: ./nginx
#         push: true
#         tags: zubairsource/mern_nginx
#      - name: build and push docker image
#      uses: docker/build-push-action@v2
#      with:
#        username: ${{secrets.DOCKER_USERNAME}}
#        password: ${{secrets.DOCKER_PASSWORD}}
#        repository: zubairsource/mern_nginx
#        dockerfile: nginx/Dockerfile
#        tags: latest